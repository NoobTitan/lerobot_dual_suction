robot:
  type: cs66
  ip: 192.168.101.11

  cameras:
    top:
      type: intelrealsense
      serial_number_or_name: 346122071690
      width: 640
      height: 480
      fps: 30
    wrist:
      type: intelrealsense
      serial_number_or_name: 937422070277
      width: 640
      height: 480
      fps: 30
    
teleop:
  type: gello
  port: /dev/ttyUSB0
  id: main_gello

display_data: true
resume: true

dataset:
  repo_id: xdd0225/cs66_record_test
  num_episodes: 20
  single_task: test_mission  # 任务类型
  episode_time_s: 60          # 每个 episode 持续 60 秒（可选）
  reset_time_s: 10             # 每次结束后等待 10 秒用于手动 reset（可选）
  push_to_hub: true        # 是否推送到 huggingface hub
  # tags: ["dual_arm_test", "test_func"]  # 可选标签
  private: true